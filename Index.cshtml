@model HomeViewModel

@await Html.PartialAsync("Carousel")

<h2>Prefered Cars of the week </h2>
<h4>Clients preferences - just for you!</h4>

<div class="row">
    @foreach (var Car in Model.PreferredCars)
    {
        @Html.Partial("CarsSummary", Car)
    }
</div>